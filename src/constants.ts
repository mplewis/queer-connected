export const SITE_NAME = 'Queer & Connected';
export const SITE_URL = 'https://example.com';

/** Calendar name for the full events feed. */
export const CALENDAR_NAME = `${SITE_NAME} Events`;

/** Calendar description. */
export const CALENDAR_DESCRIPTION = `Events from the ${SITE_NAME} community`;

/** Timezone for all calendar events. */
export const CALENDAR_TIMEZONE = 'America/Denver';
